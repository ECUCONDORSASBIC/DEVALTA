{
  "name": "@altamedica/medical-types",
  "version": "1.0.0",
  "description": "Tipos e interfaces médicos unificados para la plataforma AltaMedica",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs --format esm --dts --clean",
    "dev": "tsup src/index.ts --format cjs --format esm --dts --watch",
    "clean": "pwsh -Command \"if (Test-Path dist) { Remove-Item -Path dist -Recurse -Force }\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3"
  },
  "files": [
    "dist",
    "src"
  ],
  "keywords": [
    "medical",
    "types",
    "healthcare",
    "altamedica",
    "typescript",
    "interfaces"
  ],
  "author": "AltaMedica Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/altamedica/platform"
  },
  "sideEffects": false
}