# 📚 API Usage Guide - Easy Access to All Endpoints

## 🎯 Quick Start

```typescript
import { API } from './organized_api_config';

// Get patient list
const patientsUrl = API.url('patients', 'api_server', 'list');
// Result: http://localhost:3001/api/v1/patients

// Get specific patient
const patientUrl = API.url('patients', 'api_server', 'byId');
// Result: http://localhost:3001/api/v1/patients/:id

// Create appointment
const appointmentUrl = API.url('appointments', 'doctors', 'list');
// Result: http://localhost:3002/api/appointments
```

## 🗂️ Available Domains

### Gestión de Pacientes (`patients`)
- **Total APIs:** 12

- **api_server** (port 3001): 5 endpoints
- **patients** (port 3003): 4 endpoints
- **doctors** (port 3002): 2 endpoints
- **web_app** (port 3000): 1 endpoints

### Gestión de Doctores (`doctors`)
- **Total APIs:** 16

- **api_server** (port 3001): 16 endpoints

### Citas Médicas (`appointments`)
- **Total APIs:** 12

- **api_server** (port 3001): 8 endpoints
- **doctors** (port 3002): 3 endpoints
- **web_app** (port 3000): 1 endpoints

### Telemedicina y Videollamadas (`telemedicine`)
- **Total APIs:** 44

- **api_server** (port 3001): 40 endpoints
- **doctors** (port 3002): 3 endpoints
- **web_app** (port 3000): 1 endpoints

### Anamnesis y Historiales (`anamnesis`)
- **Total APIs:** 5

- **patients** (port 3003): 5 endpoints

### Expedientes Médicos (`medical_records`)
- **Total APIs:** 3

- **api_server** (port 3001): 3 endpoints

### Recetas Médicas (`prescriptions`)
- **Total APIs:** 8

- **api_server** (port 3001): 8 endpoints

### Reportes Médicos (`reports`)
- **Total APIs:** 4

- **api_server** (port 3001): 4 endpoints

### Empresas/Instituciones (`companies`)
- **Total APIs:** 13

- **api_server** (port 3001): 11 endpoints
- **python_tools** (port 8888): 2 endpoints

### Marketplace B2B (`marketplace`)
- **Total APIs:** 18

- **api_server** (port 3001): 17 endpoints
- **web_app** (port 3000): 1 endpoints

### Ofertas de Trabajo (`jobs`)
- **Total APIs:** 7

- **api_server** (port 3001): 7 endpoints

### Autenticación y Autorización (`auth`)
- **Total APIs:** 4

- **api_server** (port 3001): 3 endpoints
- **web_app** (port 3000): 1 endpoints

### Administración del Sistema (`admin`)
- **Total APIs:** 10

- **api_server** (port 3001): 10 endpoints

### Gestión de Usuarios (`users`)
- **Total APIs:** 8

- **api_server** (port 3001): 8 endpoints

### Pagos y Facturación (`payments`)
- **Total APIs:** 4

- **api_server** (port 3001): 4 endpoints

### Inteligencia Artificial (`ai`)
- **Total APIs:** 11

- **api_server** (port 3001): 11 endpoints

### Estado del Sistema (`health`)
- **Total APIs:** 14

- **api_server** (port 3001): 7 endpoints
- **patients** (port 3003): 1 endpoints
- **doctors** (port 3002): 1 endpoints
- **admin** (port 3005): 1 endpoints
- **companies** (port 3004): 1 endpoints
- **web_app** (port 3000): 3 endpoints

### Sistema de Matching (`matching`)
- **Total APIs:** 4

- **python_tools** (port 8888): 4 endpoints

### Otros Endpoints (`other`)
- **Total APIs:** 59

- **api_server** (port 3001): 49 endpoints
- **web_app** (port 3000): 10 endpoints

## 🔗 Common Patterns

```typescript
// Health checks
API.url('health', 'api_server', 'health')  // System health
API.url('health', 'patients', 'health')    // Patients app health

// Authentication
API.url('auth', 'api_server', 'login')     // Login endpoint
API.url('auth', 'api_server', 'refresh')   // Token refresh

// CRUD operations
API.url('patients', 'api_server', 'list')    // GET list
API.url('patients', 'api_server', 'byId')    // GET by ID
API.url('patients', 'api_server', 'create')  // POST create
API.url('patients', 'api_server', 'update')  // PUT update
```

**Total APIs Organized:** 256

---
*Generated by API Organizer - Making APIs easy to find and use*