// 🔄 Re-export del AuthProvider centralizado desde @altamedica/auth
// Este archivo mantiene compatibilidad durante la migración
export { AuthContext, AuthProvider, useAuth } from '@altamedica/auth';
export type { AuthContextType, User, UserRole } from '@altamedica/auth';

